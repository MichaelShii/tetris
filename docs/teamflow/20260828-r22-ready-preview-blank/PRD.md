<!-- meta: summary="r22 READY 态预览留白：队列仅对局中渲染（用户裁定，取代 r15 AC-1「READY 亦渲染初始 3 格」），消除未开局先展示随机序列的观感；引擎队列构造不变，预览仍为真实开局序列" -->
基线依赖：r15（三格预览队列）；**取代：r15 AC-1「READY 态亦渲染初始 3 格」**

# r22 READY 态预览留白 PRD

## 1. 背景与目标

用户反馈：未点开始游戏，三格预览已出现且每次刷新内容不同（`Math.random` 构造期生成队列所致）。经核实为 r15 AC-1 有意设计（READY 渲染初始队列，且该队列即开局真实序列，start 只是从同一队列弹出）。用户裁定改为**开始后才显示**：消除「未开局先抽卡」观感。

- **G1**：READY 态三槽留白（容器与标签保留）；start 后首帧即渲染队列；PAUSED/OVER 维持显示（对局状态的一部分）。
- **G2**：引擎零改动——队列仍在构造期生成（restart 重建语义不变），仅 UI 渲染层按 phase 门控。

## 2. AC 清单

- **AC-1 [P0]** READY 预览留白（#next-well 无绘制，e2e fill=0 实测）；开始后即渲染 3 格（fill≥12 实测，所见即所打不变）。
- **AC-2 [P0]** 预览开关关闭（AC-7/9）、restart 重建队列、PAUSED/OVER 显示等既有语义零回归；七套全绿。

## 3. 工程约束

- 改动仅 `ui.js`（渲染门控一行 + 注释）+ `qa-e2e-jsdom.cjs`（READY 断言改写为 fill=0，新增开始后 fill≥12 断言，用例总数 +1）；版本号不升；git 由用户收口。
