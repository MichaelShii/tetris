<!-- meta: summary="r19 QA 报告：七套全绿 108/24/23/15/2/ALL/366；四档竖屏视口真值零溢出+棋盘精确 1:2；横屏子变体与 r17 基线逐字段一致（stash 对照）；对局态触控链路实拍通过；环境伪信号与遗留观察 D1~D4" -->

# r19 移动端棋盘优先一屏适配 QA 报告

**测试对象**：r19 S 竖屏游戏视口重写（style.css §7.1）+ verify-ui.cjs / qa-e2e-jsdom.cjs 断言同步
**基线**：r18（HEAD 51f4954）｜**测试人**：QA 测试工程师
**测试手段**：七套 Node 脚本（jsdom/源结构断言）+ playwright-core 驱动系统 Chrome 无头真实渲染（视口精确 375×667 / 390×844 / 320×568 / 430×932 / 568×320 / 844×390 / 1280×800，`getBoundingClientRect` 真值 + 截图，工具脚本用后即弃未入库）

## 1. 自动化回归（七套全绿）

| 套件 | 结果 |
|---|---|
| verify-game / verify-audio / verify-ui / verify-persist / verify-constants | **108 / 24 / 23 / 15 / 2**，fail 全 0 |
| assembly-check | **ALL CHECKS PASSED** |
| qa-e2e-jsdom | **366/366**（含 r19 改写断言 2 条、r16/r17 回归链、file:// 管线） |

与 r18 基线（108/24/23/2/15/ALL/366）完全一致：断言改写为等量替换（verify-ui 第三条测试原地重写、e2e 前两条 check 原地重写），无新增/删除用例。

## 2. 真实视口对拍（AC-1/2/3/4 核心证据）

竖屏四档 `scroll == inner`（**零滚动、零横向溢出**），全部区块首屏可见，棋盘精确 1:2：

| 视口 | scrollW×H | 棋盘（画布） | 比例 | dock 位置（视口内） | 设置钮 |
|---|---|---|---|---|---|
| 320×568 | 320×568 ✓ | 124×248 | 1:2 ✓ | y=507 h=61 ✓ | 92×44 右上 ✓ |
| 375×667 | 375×667 ✓ | 179×358 | 1:2 ✓ | y=606 h=61 ✓ | ✓ |
| 390×844 | 390×844 ✓ | 194×388 | 1:2 ✓ | y=779 h=65 ✓ | ✓ |
| 430×932 | 430×932 ✓ | 234×468 | 1:2 ✓ | y=867 h=65 ✓ | ✓ |

- 统计行 [12,60,351,42]（375 档）单行四列 + 数值 18px；控制行 [12,110,351,44]。
- 触控 dock 单行 6 键随流（position:static）：320 档 44px 键 + 8px 键距 + 8px 边距 = 320 恰容 ✓（AC-3 键距红线保持）。
- **对局态实拍**（375×667，tap 开始 → 左移×6 → 旋转）：PLAYING 状态灯、方块活动、幽灵块落点、Next 三格队列、dock 按键全部生效。截图为过程产物未入库；如需复现，用 playwright-core + 系统 Chrome 无头按本节视口矩阵驱动即可（脚本要素：viewport 375×667 / hasTouch / `#btn-start` 点击后 `.tkey[data-action]` 连点）。
- 键位图例 S 竖屏不渲染（AC-8）；Hold/Next 侧栏贴棋盘两侧，画布内联尺寸 48×24/48×80 不变（AC-5，`#next-well` 48×80 e2e 断言沿用通过）。

## 3. 其余档位零回归（AC-6）

- **git diff 审查**：§7.2（S 横屏变体）/ §7.3 / §7.4（M）/ 全部 L 基线规则 0 行 diff；改动仅 §7.1 原地重写 + 头注。
- **横屏子变体 stash 对照**（568×320，r17 基线 vs r19 同探针）：scroll [644,581]、main [0,-146,568,521]、stats/board/settings 逐字段**完全一致** → 横屏行为与 r17 交付等同（其内容溢出为 r17 既有缺陷，见 D1）。
- 横屏卡片玻璃底（原 r17 §7.1 外层横竖屏通用）已原样收口至本档 landscape 嵌套，保真复刻。
- L 档 1280×800 探针：main/panels/board 280×560 几何正常。**注意**：无头 Chrome 固定上报 `navigator.maxTouchPoints=10`（真实键鼠桌面为 0）→ 探针中 has-touch 类被误点亮、dock 显示——**环境伪信号非缺陷**：产品检测逻辑 0 改动，jsdom e2e「无触屏能力 → html 无 has-touch」断言通过，r16 桌面零视觉变化回归链全绿；触屏本上报 10 属 r16 设计内行为（R1~R10 之混合设备项）。

## 4. 行为变更与缺陷登记

| 编号 | 级别 | 内容 | 处置 |
|---|---|---|---|
| BC-1 | 已确认变更 | 键位图例 S 竖屏隐藏（含非触屏窄窗口；触屏本就隐藏） | 细化设计已获用户确认（PRD AC-8） |
| D1 | P3（r17 既有） | S 横屏变体 568×320 内容 644×581 > 视口（board max-height 150px 占位量未校准） | 与 r17 待办 D2 同族合并跟踪；非本迭代引入（stash 对照证据） |
| D2 | P3（r17 既有） | 横屏手机 844×390 命中 M 三列档（M 无横屏感知），棋盘固有 560px 高纵向溢出（scrollH 757） | 下轮可议：M 档补矮视口（max-height）变体；非本迭代引入 |
| D3 | P3（设计权衡） | 宽屏手机棋盘 width-bound（430 档 234px），板框纵向留白（框高 100% 使然，遮罩随框覆盖） | 后续可议 Next 队列顶部横排（需 ui.js 双模式渲染 + L 档重基线，成本见 PRD §1） |
| D4 | P3（继承 r17 D3） | M 档 `--dock-h` 64+inset 自动化断言仍缺（r19 移除的仅 S 档两行算式断言） | 随 r17 待办继续跟踪 |

**人工补测（真机，沿用既有清单集中处理）**：safe-area inset=34 刘海遮挡、`100dvh` 动态工具栏收缩、多指并行、dock 拇指手感、横屏 568×320 校准（D1）、竖屏四档字号截断复核（AC-4）。

## 5. 结论

**✅ 通过（QA 出口）**：七套全绿、竖屏一屏适配四档真值全达标、其余档位零回归（含横屏逐字段对照）、对局态触控链路实拍通过；无 P0/P1/P2 缺陷，P3 登记 4 项（2 项为 r17 既有顺带核实）。
