# docs/teamflow/memory.md — 产品记忆（迭代历史与待办）

> 维护者：TeamFlow 研发流水线（产品经理每次迭代更新；AGENTS.md §5/§6 为精炼锚点，本文件为记忆落盘）。
> 本文件承载**新迭代需求与目标版本**等面向下一次研发的产品记忆；历史细节以 `docs/history/<版本>/` 快照追溯。

## 迭代历史（产品记忆）

| 版本 | 日期 | 需求 | 结果 |
|---|---|---|---|
| v1.0 | 2026-08-16 | 简易俄罗斯方块：闭环玩法、计分、难度升级、科技玻璃风格 | ✅ 交付完成；QA 功能验收通过 |
| v2.0 | 2026-08-16 | 音效系统（七类合成音效）+ 音量/静音控制（M 键 + 面板按钮），零外部音频文件 | ⚠️ 有条件通过；AC-01~10 可自动化项 100% 全绿，无 P0/P1/P2 缺陷；人工补测待完成 |
| v2.1 | 2026-08-16 | 暂停/继续快捷键：`P` 双向切换 + `空格` 暂停态继续 + GAME_OVER `空格` 重开；零 UI 视觉改动 | ⚠️ 有条件通过；AC-01~11 可自动化项 100% 全绿（47/19/6/装配/164），无 P0/P1/P2 缺陷；人工补测待完成 |
| **v2.2** | 2026-08-16 | 幽灵块（落点预览）：引擎新增纯函数落点计算（复用 `collides`、无踢墙、垂直直线）+ UI 半透明轮廓渲染（同色系、可辨识）；随移动/旋转/软降/自动下落实时刷新；**不改数值/状态机/keyAction/音效**；回归底线 AC-01~11 | ⚠️ 有条件通过（产品验收 2026-08-16）：AC-12 可自动化项 100% 通过（51/19/7/装配/177，独立复跑一致），与硬降落点偏差 0 格自动化断言；AC-01~11 回归全绿；无 P0/P1/P2 缺陷；1 项 P3 观察项 OBS-12-1（E-12-08 防御未实现，不阻断）；AC-12.11 幽灵块真机目测/双分辨率/复杂形状/相邻边界 4 项人工补测待完成（QA-REPORT §6） |
| **v2.2-tc**（tech） | 2026-08-16 | 工程自检脚本 `verify-constants.cjs`：断言三模块头部 `VERSION` 均 === `'2.2.0'` 且与 TECHNICAL §2.2 一致；仅新增测试脚本，**不改任何既有代码/行为/UI/AC** | ✅ 技术变更单关闭（产品验收 2026-08-16）：新脚本 2/2 绿 + 回归底线五套全绿（**51/19/7/装配/177**，v2.2 计数，非 47/19/6）；QA 负向对抗证明脚本真实区分力（false-green 防护）；无 P0/P1/P2 缺陷；1 项 P3 观察项 OBS-TC-1（changes.md §5 基数引 v2.1 旧计数 47/19/6，实际 51/19/7，文档口径待下次同步更正） |

## 当前迭代记忆（v2.2，幽灵块）

> **✅ 已交付（2026-08-16 产品验收 ⚠️ 有条件通过）**：AC-12 可自动化项 100% 通过、AC-01~11 回归全绿、无 P0/P1/P2 缺陷；验收条件 = 下方「人工补测（v2.2 验收新增）」全部通过。OBS-12-1（P3 文档-实现对齐，非功能缺陷）已登记待后续收紧 `ghostY` 健壮性。

> **✅ 技术改造（v2.2-tc，tech 无功能变化，已验收关闭 2026-08-16）**：新增工程自检脚本 `scripts/verify-constants.cjs`——断言 `game.js`/`ui.js`/`audio.js` 头部 `VERSION` 均 === `'2.2.0'` 且与 `docs/technical/TECHNICAL.md` §2.2 记录一致；仅新增测试脚本，**不改任何既有代码/行为/UI/AC**。技术变更单 `docs/technical/changes.md` 已按契约束关闭，QA 独立复跑全绿（回归底线 **51/19/7/装配/177**），负向对抗证实脚本能捕获单模块版本漂移（非 false-green）。遗留 P3 OBS-TC-1：changes.md §5 回归底线表沿用 v2.1 旧计数 47/19/6/177，实际为 51/19/7/177，文档口径待下次同步更正。

> **🗂 交付补记（2026-08-16，卫生项/文档同步确认验收）**：AGENTS.md §1/§5/§6/§7 同步 v2.2 + `game.js`/`ui.js`/`audio.js` 头部 `VERSION` 统一为 `'2.2.0'`（TECHNICAL §2.2 已同步），**代码零行为改动**；QA-REPORT §7.1 五套验证独立复跑全绿（51/19/7/装配/177），AC-01~12 可自动化项 100% 通过、无 P0/P1/P2 缺陷 → **接受**（条件同 v2.2 人工补测清单）。PRD 结构无变化，SUMMARY 无需改。

- **目标版本**：v2.2（已交付）
- **需求摘要**：P2 增强「幽灵块（落点预览）」——当前下落方块正下方以半透明轮廓实时显示垂直落点预览，跟随左右移动/旋转/软降实时刷新。
- **交付要点（AC-12）**：
  1. 引擎层**新增纯函数** `ghostY(board, piece)`，复用既有 `collides(board, piece)` 碰撞语义（**不改其实现**），无踢墙、垂直直线下落；与 `hardDrop` 落点循环逐格同语义 → 偏差 0 格（AC-12.1 自动化断言）。
  2. UI 层 `drawGhost` **新增半透明渲染**（`GHOST` 参数表：OUTLINE_ALPHA 0.75 / FILL_ALPHA 0.16 / LINE_WIDTH 2，同色系、暗色玻璃基调可辨识），层级介于固定块与实体块之间。
  3. **不得改动**既有数值（§5）、状态机（四态迁移）、快捷键映射（`keyAction`）、音效触发（AC-09）；脚本仅新增纯函数与渲染，零改动确认。
  4. 回归底线 **AC-01 ~ AC-11 全绿**（51/19/7/装配/177）。
  5. UI 视觉（幽灵块可辨识度/双分辨率/复杂形状落点/相邻边界目测）归**人工补测清单**（环境限制非缺陷，AC-12.11）。

## 待办（下一批，不含本期）

> **⚠️ 文档标点热修未交付（产品验收 2026-08-16 ❌ 不通过，需返工）**：单点热修——`README.md` 第 37 行操作说明表 `| `P` 或 `Esc` | 暂停 / 继续（双向切换） |` 中的 `/`（含两侧空格）应改正为顿号 `、`，即 `暂停、继续（双向切换）`（中文并列标点规范，纯文档、零代码改动、不升版本号）。**验收时复核发现：改动未落地，README 第 37 行仍为 `暂停 / 继续`——开发轮误按旧 v2.2 幽灵块范围核查并输出"本轮无需新增代码改动"结论，未执行本热修。** 待开发单行修改 `README.md` L37 并复跑 `node scripts/assembly-check.cjs`（基线 ALL CHECKS PASSED）后再验收。

- **P2 增强**（本期未做）：背景音乐（BGM）、7-bag 随机、最高分本地持久化、触屏操作、音量/静音本地持久化、消行动画缓动。（~~幽灵块（落点预览）~~ → **v2.2 已交付**）
- **人工补测（v2.2 验收新增，QA-REPORT §6，环境限制非缺陷）**：幽灵块在暗色玻璃基调下可辨识度（透明度 0.16/0.75、线宽 2、色相）；旋转后复杂形状（I/S/Z）落点目测；1920×1080 与 1366×768 双分辨率无错位；与其他块体相邻时边界可辨识；刷新及时性/FPS≥55（AC-12.8/12.11）。
- **人工补测**（v2.1/v2.0 遗留，QA-REPORT §6）：快捷键时序、音效听感/时序/性能、自动播放、双分辨率布局、离线多浏览器、读屏等项待完成。
- **测试增强（P3 建议）**：E2E 补 AC-09.6 显式断言（首次交互后 AudioContext `resume` 调用次数 = 1）；`#btn-mute` 静音态 `aria-label` 动态切换为「取消静音」。
- **OBS-12-1（P3，文档-实现对齐，非功能缺陷）**：TECHNICAL §6.2 E-12-08 描述 `ghostY` 应对非法入参防御（`rot%4` 归一、未知 type 回退），实际 `ghostY` 复用 `collides`/`shapeOf` 对 `rot>3`/未知 type 抛错；不影响任何 AC-12 验收项（唯一调用方 ui.js 已守卫合法 piece），分类为文档/健壮性对齐缺口，后续可在 `ghostY` 加 `rot%4` 归一并补 E-12-08 用例后关闭。
- **OBS-TC-1（P3，文档口径偏差，v2.2-tc 新发现，不影响验收）**：`docs/technical/changes.md` §5 回归底线表沿用 v2.1 旧计数 **47/19/6/177**，实际当前套件为 **51/19/7/177**（v2.2 计数：game 51、ui 7）；`AGENTS.md` §4 亦列 47/6（旧），§5 v2.2 行正确列 51/7。仅影响文档数字口径，回归本身 100% 全绿；待下次文档同步时更正 changes.md §5 与 AGENTS.md §4 为 51/19/7/177。
