# AGENTS.md — 团队协作守则与产品记忆锚点（tetris 产品线）

> **任何新加入本产品的 Agent（团队成员）必须先通读本文件**，再按 §2 文档索引读取相关文档与任务卡片，
> 不要在未了解产品现状前自行全量探索代码。
> 维护者：TeamFlow 研发流水线（架构师起草与更新；产品经理每次迭代核对 §5 产品记忆与 §6 待办）。

---

## 1. 产品是什么

- **产品**：俄罗斯方块（Tetris）简化版 —— 自包含静态 Web 小游戏，科技玻璃风格（深色 + 毛玻璃 + 霓虹）。
- **产品根**：`products/tetris/`（工作区产品线约定：`products/<product>/`）。
- **交付形态**：双击 `index.html` 即玩（`file://`），零构建、零外部依赖、离线可用。
- **当前版本**：v2.2（2026-08-16 交付，产品验收 ⚠️ 有条件通过：AC-01~12 可自动化项 100% 全绿（51/19/7/装配/177 独立复跑一致）、无 P0/P1/P2 缺陷；人工补测清单待完成，见 §6）。
- **进行中**：—（v2.2 幽灵块已交付；下一批见 §6 已知待办：人工补测清单 + P2 增强项）。
- **游戏闭环**：开始 → 游玩 → 得分 → 难度升级 → 结束 → 重新开始；含暂停/恢复、失焦自动暂停；当前下落方块正下方实时显示幽灵块（落点预览）半透明轮廓。

## 2. 文档索引（按职责）

| 职责 | 位置 | 说明 |
|---|---|---|
| **摘要索引** | `docs/SUMMARY.md` | **先读**：每文档用途/关键章节/读取指引（token 预算） |
| 产品入口 | `README.md` | 玩法、操作、运行方式、验收速览 |
| 需求（PRD） | `docs/prd/PRD.md` | **验收唯一依据**：AC-01~11 + §5 数值规格（计分/等级/速度表/音效规格） |
| 设计 | `docs/design/DESIGN.md` | 视觉 token / 交互 / 动效 / 可访问性规范 |
| 架构 | `docs/architecture/ARCHITECTURE.md` | 工程方案；注意：TS+Vite 为**可选升级路径**，实际交付为扁平纯 JS |
| 技术方案 | `docs/technical/TECHNICAL.md` | 模块接口契约（UMD 对齐）与任务拆分参考 |
| QA | `docs/qa/QA-REPORT.md` | 测试报告 + 人工补测清单（v2.1 章 §6；v2.0 历史章 §6.2） |
| 历史归档 | `docs/history/<版本>/` | 已发布版本的文档快照（日常不读，仅供追溯） |
| 待办 | 本文件 §6 | 下一批需求与遗留事项 |

## 3. 团队角色与标准流程

**角色**：产品经理 / UI/UX 设计师 / 架构师 / 高级全栈工程师 / QA 测试工程师。

**标准流程**（TeamFlow 流水线）：
`需求 → PRD（产品经理）→（UI 改造时）UI/UX 设计 →（新项目时）架构规划 → 技术方案（高级全栈）→ 并行开发 → QA 功能测试 → 产品验收（产品经理）`

**产出物落盘约定**（新迭代必须遵守）：
| 环节 | 落盘位置 |
|---|---|
| PRD | `docs/prd/PRD.md`（迭代时升级版本号 + 修订记录） |
| 设计 | `docs/design/DESIGN.md` |
| 架构 | `docs/architecture/ARCHITECTURE.md` |
| 技术方案 | `docs/technical/TECHNICAL.md` |
| QA 报告 | `docs/qa/QA-REPORT.md` |
| 产品现状 | 更新本文件 §1/§5/§6 |

**完成度自查**：每个环节交付前必须对照自身职责清单自查，**未完成不得流转到下一环节**；
架构师对新项目还必须**实际初始化脚手架文件**（目录树、package.json 等）与 AGENTS.md 草稿，而不只是输出方案文档。

## 4. 工程约定（本产品）

- **代码形态**：扁平纯 JS（`game.js` / `audio.js` / `ui.js` / `style.css` / `index.html`），UMD 契约（`window.TetrisGame` / `window.TetrisAudio` / `window.TetrisUI`），零构建、零运行时依赖。
- **数值单一事实来源**：`game.js` 顶部常量（对应 PRD §5）+ `audio.js` 顶部 `SFX_DEFS`（对应 PRD §5.2/TECHNICAL §2.2）；改动必须同步更新 `scripts/verify-game.cjs` / `scripts/verify-audio.cjs` 单测。
- **验证命令**（产品根下执行）：
  - `node scripts/verify-game.cjs`（引擎 47 项，含 onSfx 事件序列）
  - `node scripts/verify-audio.cjs`（音效引擎 19 项，v2.0 新增）
  - `node scripts/verify-ui.cjs`（UI 契约 6 项）
  - `node scripts/assembly-check.cjs`（装配 + 自包含审计 + 音频文件审计）
  - `node scripts/qa-e2e-jsdom.cjs`（164 项 DOM E2E，需 jsdom）
- **风格**：纯函数优先、工厂函数 + 闭包（不用 class）、不可变棋盘、`dispose()` 统一清理。

## 5. 产品记忆（迭代历史）

| 版本 | 日期 | 需求 | 结果 |
|---|---|---|---|
| v1.0 | 2026-08-16 | 简易俄罗斯方块：闭环（开始/暂停/结束/重开）、消行计分、难度升级、科技玻璃风格 | ✅ 交付完成；QA 功能验收通过（33+5+装配+108 全绿）；视觉/性能待人工补测 |
| v2.0 | 2026-08-16 | 音效系统：Web Audio API 合成七类事件音效（移动/旋转/软降/硬降/消行/升级/结束）+ 音量/静音控制（M 键 + 信息面板按钮），零外部音频文件 | ⚠️ 有条件通过（产品验收 2026-08-16）：AC-01~10 可自动化项 100% 全绿（44+19+6+装配+139，独立复跑一致），无 P0/P1/P2 缺陷；听感/延迟/FPS/双分辨率布局/断网/多浏览器/读屏等 7 项人工补测待完成（QA-REPORT §6.2） |
| v2.1 | 2026-08-16 | 暂停/继续快捷键：`P` 双向切换（既有语义保留）+ `空格` 暂停态「继续」（PLAYING 态仍为硬降）+ GAME_OVER `空格` 重开（D-01 方案甲）；window 级监听、任意焦点可用；**零 UI 视觉改动** | ⚠️ 有条件通过（产品验收 2026-08-16）：AC-01~11 可自动化项 100% 全绿（47/19/6/装配/164，独立复跑一致），无 P0/P1/P2 缺陷；真机时序/AC-11.7 截图对比/焦点真机 3 项（v2.1 新增）+ v2.0 遗留 7 项人工补测待完成（QA-REPORT §6） |
| v2.2 | 2026-08-16 | 幽灵块（落点预览）：引擎纯函数 `ghostY` 落点计算（复用 `collides`、无踢墙、垂直直线）+ UI 半透明轮廓渲染（同色系、随移动/旋转/软降/自动下落实时刷新）；**不改数值/状态机/keyAction/音效** | ⚠️ 有条件通过（产品验收 2026-08-16）：AC-12 可自动化项 100% 通过（51/19/7/装配/177，独立复跑一致）、与硬降落点偏差 0 格自动化断言；AC-01~11 回归全绿；无 P0/P1/P2 缺陷；1 项 P3 观察项 OBS-12-1（E-12-08 防御未实现，不阻断）；幽灵块真机目测/双分辨率/复杂形状/相邻边界 4 项人工补测待完成（QA-REPORT §6） |

## 6. 已知待办（下一批）

- ~~**v2.2 迭代中**（幽灵块落点预览，AC-12）~~ → **✅ 已交付（2026-08-16 产品验收 ⚠️ 有条件通过）**：引擎 `ghostY` 纯函数 + UI 半透明轮廓渲染；AC-12 可自动化项 100% 通过（51/19/7/装配/177）、与硬降偏差 0 格自动化断言、AC-01~11 回归全绿、无 P0/P1/P2 缺陷；验收条件 = 下方「人工补测（v2.2 验收新增）」全部通过；OBS-12-1（P3）已登记。

- ~~**v2.1 迭代中**（PRD 已评审，待技术方案/开发/QA）：暂停/继续快捷键——`P` 双向切换（既有语义保留）+ `空格` 暂停态继续（新增；PLAYING 态 `空格` 仍为硬降），AC-11；**零 UI 视觉改动**；回归底线 AC-01~10~~ → **✅ 已交付（2026-08-16 产品验收 ⚠️ 有条件通过）**：验收条件 = 下方「人工补测（v2.1 验收新增）」全部通过。

- ~~**v2.0 迭代中**（PRD 已评审，待技术方案/开发/QA）~~ → **✅ 已交付（2026-08-16 产品验收 ⚠️ 有条件通过）**：音效系统（七类事件合成音效，AC-09）+ 音量/静音控制（M 键 + 面板按钮，AC-10）；验收条件 = 下方「人工补测（v2.0 验收遗留）」全部通过。
- **人工补测（v2.2 验收新增，QA-REPORT §6，环境限制非缺陷）**：
  1. 幽灵块可辨识度：暗色玻璃基调下透明度 0.16/0.75、线宽 2、色相可辨识（AC-12.8）；
  2. 旋转后复杂形状（I/S/Z）落点目测：各旋转态幽灵落点与硬降固定位置对齐（AC-12.3）；
  3. 1920×1080 / 1366×768 双分辨率无错位：幽灵块与实体块/网格/面板对齐（AC-12.8）；
  4. 与其他块体相邻时边界可辨识（含低/高堆积场景，AC-12.11）；
  5. 刷新及时性/性能：移动/旋转/软降后幽灵即时刷新 ≤100ms、含幽灵渲染 FPS≥55（AC-12.10）。
- **人工补测（v2.1 验收新增，QA-REPORT §6，环境限制非缺陷）**：
  1. 快捷键真实时序：DevTools/Performance 采样 `P`/`空格` 状态切换 ≤300ms；恢复后首个自动下落间隔与等级表偏差 ≤50ms（AC-11.1/2/4 真机复核）；
  2. AC-11.7 截图对比：v2.0 与 v2.1 同屏截图逐像素对比无差异（遮罩/信息面板/按钮/key-hints）；
  3. 焦点场景真机复核：Tab 遍历至各按钮后按空格/P，确认与 jsdom 断言一致（AC-11.5）。
- **人工补测（v2.0 验收遗留，QA-REPORT §6.2，环境限制非缺陷）**：
  1. 听感：七类音效两两可区分（AC-09.1）；静音后 0 可闻输出、关闭立即恢复（AC-10.3）；任意音量（含 0%/100%）无爆音/削波（AC-10.4）；
  2. 时序：DevTools Performance 事件→发声 ≤50ms；消行/升级/结束音效与视觉同步（AC-09.4/§1.3）；
  3. 性能：60s 高频操作后 Console 0 报错、Memory 无节点泄漏、FPS≥55；首屏 ≤500ms；完整游玩 5 分钟 0 报错（AC-09.8/§1.3）；
  4. 自动播放：刷新后先按任意键/点击，音效开始正常、之前无报错（AC-09.6）；
  5. 布局：音量控件在 1920×1080 / 1366×768 无错位、无遮挡（AC-10.1/AC-07.4）；
  6. 离线与多浏览器：Network→Offline 完整游玩一轮；Firefox 最新版双击可玩（AC-08）；
  7. 读屏：NVDA/VoiceOver 读出静音状态（aria-pressed 切换，AC-10.6）。
- **测试增强（P3 建议，非阻断）**：E2E 补 AC-09.6 显式断言（首次交互后 AudioContext `resume` 调用次数 = 1，OBS-6）；`#btn-mute` 静音态将 `aria-label` 动态切换为「取消静音」（OBS-7，读屏语境细化）。
- ~~**卫生项（P3，OBS-11-3，可选）**：`game.js`/`ui.js` 头部 `VERSION` 常量未升 '2.1.0'~~ → **✅ 已处理（2026-08-16）**：`game.js`/`ui.js`/`audio.js` 三模块头部 `VERSION` 统一升 `'2.1.0'`（TECHNICAL §2.2 无断言依赖；改动后 47/19/6/装配/E2E 复跑全绿）。
- **P2 增强**（本期未做）：背景音乐（BGM）、7-bag 随机、最高分本地持久化、触屏操作、音量/静音本地持久化。（~~幽灵块（落点预览）~~ → **v2.2 已交付**）
- **OBS-12-1（P3，文档-实现对齐，不阻断）**：TECHNICAL §6.2 E-12-08 描述的 `ghostY` 非法入参防御（`rot%4` 归一/未知 type 回退）未实现；不影响任何 AC（唯一调用方 ui.js 已守卫合法 piece），后续加归一并补 E-12-08 用例后关闭。
- **OBS-1 已修复**：高 DPI 首帧画布按 DPR 烘焙（ui.js 初始化 resize）。
- `backlog/` 目录已就位（QA 于 2026-08-16 补齐实际目录）：阶段 2 起承载需求/任务/缺陷卡片（任务状态机）。

## 7. 变更记录

- 2026-08-16：创建本文件；文档收口至产品线结构（`products/tetris/`，docs 按职责分类）；补 ARCHITECTURE/TECHNICAL 实际交付偏差注记；修复 OBS-1。
- 2026-08-16：v2.0 迭代启动；PRD 升级 v2.0（保留 AC-01~08，新增 AC-09 合成音效 / AC-10 音量与静音控制）；§1 进行中、§5 产品记忆、§6 待办同步。
- 2026-08-16：v2.0 产品验收（产品经理）：独立复跑四套验证 + E2E 全绿（44/19/6/装配/139），AC-01~10 可自动化项 100% 通过、无 P0/P1/P2 缺陷 → ⚠️ 有条件通过；§1 当前版本、§5 产品记忆、§6 待办同步（人工补测清单 + P3 观察项 OBS-6/7 登记）；OBS-4 关闭。
- 2026-08-16：v2.1 迭代启动（产品经理）：PRD 升级 v2.1（保留 AC-01~10 编号与语义、压缩旧表述，新增 AC-11 暂停/继续快捷键：P 双向切换 + 空格暂停态继续；不改 UI 视觉）；归档 v2.0 快照至 `docs/history/v2.0/`；§1 进行中、§2 文档索引、§6 待办同步。
- 2026-08-16：v2.1 技术方案落盘（高级全栈工程师）：TECHNICAL 升级 v2.1（键盘映射单一来源表 `keyAction` 导出 + PAUSED 空格=继续 + GAME_OVER 空格=重开【决策 D-01，QA 前待 PM 确认】+ 单测/E2E/装配审计增量）；v2.0 TECHNICAL 快照归档至 `docs/history/v2.0/`；状态机/数值/audio.js/ui.js/index.html/style.css 零改动（AC-11.7）。
- 2026-08-16：v2.1 开发交付（高级全栈工程师）：`game.js` 新增 `keyAction` 键盘映射单一来源表 + `onKeyDown` 行为等价重构（PAUSED 空格=继续、OVER 空格=重开【D-01 甲】）；单测 44→47、E2E 139→164；DESIGN.md 同步；状态机/数值/audio.js/ui.js/index.html/style.css 零改动（AC-11.7）。
- 2026-08-16：v2.1 QA 测试（QA）：五套验证全绿（47/19/6/装配/164）+ 独立对抗抽查 22/22；AC-11 七条可自动化项 100% 通过、AC-01~10 回归全绿；无 P0/P1/P2/P3 缺陷；OBS-11-1（D-01 甲）/OBS-11-2（文档待 PM 同步）/OBS-11-3（VERSION 可选）登记；报告落盘 `docs/qa/QA-REPORT.md`。
- 2026-08-16：v2.1 产品验收（产品经理）：独立复跑五套验证全绿 + keyAction 矩阵 14 例抽查全过；AC-01~11 可自动化项 100% 通过、无 P0/P1/P2 缺陷 → ⚠️ 有条件通过（条件 = §6 人工补测清单）；确认 D-01 方案甲（OVER 空格=重开，与 PRD §4/AC-11.6/README 三方一致），OBS-11-1 关闭；§1/§2/§4/§5/§6/§7 同步，SUMMARY.md 同步。
- 2026-08-16：卫生项处理（OBS-11-3 关闭）：`game.js`/`ui.js`/`audio.js` 头部 `VERSION` 常量 2.0.0→2.1.0 统一升级（TECHNICAL §2.2 标注可选、无断言依赖）；四套核心验证复跑全绿 + E2E 复跑，零行为改动；§6 卫生项条目标记完成。
- 2026-08-16：AGENTS.md 同步 v2.2（高级全栈工程师）：v2.2 幽灵块已交付；§1 当前版本/进行中/游戏闭环升 v2.2；§5 新增 v2.2 行；§6 幽灵块从 P2 待办划掉 + 交付记录 + v2.2 人工补测清单 + OBS-12-1 登记；§7 补记；`game.js`/`ui.js`/`audio.js` 头部 `VERSION` 统一为 `'2.2.0'`（TECHNICAL §2.2 已同步，无断言依赖，零行为改动）。
